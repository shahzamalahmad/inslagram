<div style="margin-left: 30%; margin-right: 20%;">


      <div id="articles">
        <% @articles.each do |article| %>
          <div class="border", style="margin-top: 50px;background-color: mintcream;">

            <div id="<%= dom_id article %>">
              <div style="background-color: white; height: 50px;">
                <strong>
                  <%= current_user.username %>
                </strong>
                <%= link_to "D", article, method: :delete %>
                
              </div>
              
              <div class="text-center">
                <p class="image-fluid", style="margin-top:20px;">
                  <%= link_to image_tag article.image_url if article.image %>
                </p>
              </div>
              <div class="container">
                <div class="row">
                  <div class="col-1">
                      like
                  </div>
                  <div class="col-2">
                      <%= link_to "comment", new_article_path(article) %>
                  </div>
                  <div class="col-1">
                    share
                  </div>                
                </div>     

                <p>
                   <strong>
                    <%= "130,074 likes" %>
                  </strong><br>
                  <strong>
                    <%= current_user.username %>
                    
                  </strong>
                   <%= article.caption %>
                </p>
              </div>
            </div>

            <p>
              <%= link_to "view all comments", article_path(article) %>
            </p>
            <div class="border-bottom">
              <%= time_ago_in_words(article.created_at) %>
            </div>
                  <%= form_with model: [ article, article.comments.build ] do |form| %>
                      <%#= form.text_field :current_user.username %>
                      <%= form.text_field :body, class:"border-0", style:"width: 85%; height: 50px;", placeholder:"add a comment" %>

                      <%= form.submit value:"post", class:"btn btn-outline-primary" %>
                  <% end %>
            </div>
          </div>
        <% end %>
      </div>
  </div>
